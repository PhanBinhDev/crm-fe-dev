export enum WorkspaceVisibility {
  PRIVATE = 'private',
  PUBLIC = 'public',
}

export enum MemberRole {
  OWNER = 'owner',
  ADMIN = 'admin',
  MEMBER = 'member',
}

export enum MemberStatus {
  ACTIVE = 'active',
  PENDING = 'pending',
  REJECT = 'reject',
}
